# Configuraci�n del servidor
# server.port=8080

# Configuraci�n de la conexi�n a la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/bankapp?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=ironhack
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuraci�n de Hibernate (JPA)
#spring.jpa.hibernate.ddl-auto=update para actualizar la base de datos que ya existe
#spring.jpa.hibernate.ddl-auto=create-drop para borrar la que ya existe y crear una desde 0, muy usado en test.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Configuraci�n de logs
# logging.level.org.springframework=INFO
# logging.level.com.tu_proyecto=DEBUG

# Configuraci�n de Internacionalizaci�n (opcional)
# spring.messages.basename=messages

# Configuraci�n de perfiles (opcional)
# spring.profiles.active=dev

# Configuraci�n de CORS (opcional)
# Permitir todas las solicitudes desde cualquier origen
# spring.mvc.cors.allowed-origins=*
# Permitir m�todos espec�ficos (GET, POST, etc.)
# spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE
